spring.application.name=rentalService


#JWT configuration
#jwt.secret=${JWT_SECRET}              # 256-bit base64 or long random string
jwt.secret=TG9jYWxEZXZFbmdpbmVFeGFtcGxlU2VjdXJlSldUS2V5MTIzNDU2
jwt.access-token.expiration=3600000   
jwt.expiration=900000                  
jwt.refresh-token.expiration=2592000000 
security.inactivity-days=30         



#MySQL database configuration
spring.datasource.url=jdbc:mysql://103.14.99.198:3306/Rental_Service_DB?
#createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=9QT2{gZvm[Gx~b
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#JPA hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# Avoid Spring Integration JDBC initializer failure when DB type can't be detected
spring.integration.jdbc.initialize-schema=never

spring.main.allow-circular-references=true
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=215MB


#Server Configurationl
server.port=8080
#server.servlet.context-path=/api

#CORS configuration
#---Replace with React native app's URL
spring.web.cors.allowed-origins=
spring.web.cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# --- Logging Configuration ---
logging.level.com.rentalservice=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# File Upload Configuration
#file.upload-dir=./uploads
#upload.directory=/usr/local/tomcat9/webapps/BikeApp/uploads
#banner.storage.path=uploads/banners
#card.storage.path=uploads/card
#vehicle.storage.path=uploads/vehicles

# Base upload directory inside Tomcat webapps
file.upload-dir=/usr/local/tomcat9/webapps/RentService/uploads
upload.directory=/usr/local/tomcat9/webapps/RentService/uploads

# Subfolders under that base path
# NOTE: keep these relative to `upload.directory` so deployments can change the context/WAR name safely.
banner.storage.path=${upload.directory}/banners
card.storage.path=${upload.directory}/card
vehicle.storage.path=${upload.directory}/vehicles


# Razorpay
razorpay.key_id=
razorpay.key_secret=
razorpay.webhook_secret=

# Rate Limiting
otp.rate.limit=3
otp.rate.limit.window-minutes=10

spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
